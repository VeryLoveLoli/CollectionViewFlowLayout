import XCTest
@testable import CollectionViewFlowLayout

final class CollectionViewFlowLayoutTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(CollectionViewFlowLayout().text, "Hello, World!")
    }
}
